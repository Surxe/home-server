# Real secret env files are NEVER committed (templates *.env.example are kept).
*.env
!*.env.example
backup.env
valheim.env
# Transient mod download cache (DLLs themselves are tracked in valheim/dll/).
valheim/mod-cache/
# OS / editor junk
*.swp
*~
.DS_Store
